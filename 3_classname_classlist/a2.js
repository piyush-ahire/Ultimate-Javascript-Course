first.className = "text-black red" // changes the class of the div with that id

console.log(first.classList); //gives the list of all classes

first.classList.remove("red") //to remove that class

first.classList.add("red") //to add the class

first.classList.toggle("red") //to add and remove the class
first.classList.toggle("red") //to add and remove the class


console.log(first.classList.contains("red"));

